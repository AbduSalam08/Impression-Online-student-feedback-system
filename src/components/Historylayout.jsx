import React from "react";
import { Outlet } from "react-router-dom";

const Historylayout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default Historylayout;
